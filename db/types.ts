export interface Talks {
    id: number;
    date: string;
    name: string;
    description: string;
    state: string;
}

export interface User {
    id: number;
    name: string;
    email: string;
}